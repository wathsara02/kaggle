# Local Windows CPU setup.
#
# Use this on machines without an NVIDIA CUDA GPU. This is the right setup for
# a Ryzen 5 5600G + RX 6700 XT on normal Windows PyTorch.

--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple

torch>=2.1.0
pettingzoo>=1.24.0
gymnasium>=0.29.0
numpy>=1.24.0
pyyaml>=6.0
pytest>=7.0
matplotlib>=3.7.0
pandas>=2.0.0
