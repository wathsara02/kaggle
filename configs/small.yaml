# Fast CPU smoke test.
seed: 0
device: cpu

model:
  recurrent_hidden_size: 64
  critic_hidden_size: 64

algo:
  batch_size: 64

training:
  episodes: 20
  num_envs: 1
  exp_name: small_cpu_demo

reward_shaping:
  enabled: true
  trick_reward: 0.1
  illegal_action_penalty: -0.1
